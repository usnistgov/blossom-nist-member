{
  "name": "blossom_query_lambda",
  "private": "true",
  "version": "1.0.3",
  "description": "Blossom query Lambda",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "prebuild": "rm -fr dist",
    "build-esbuild": "esbuild src/index.ts --bundle --minify --sourcemap --platform=node --target=es2020 --outfile=dist/index.js",
    "build-ncc": "ncc build src/index.ts -o dist",
    "build": "tsc; cp --recursive node_modules/ dist/"
  },
  "author": {
    "name": "Nikita Wootten",
    "email": "nikita.wootten@nist.gov"
  },
  "license": "NIST-PD-fallback",
  "dependencies": {
    "aws-sdk": "^2.1154.0",
    "fabric-network": "^2.2.15",
    "yaml": "^2.1.1"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.100",
    "@types/node": "^17.0.42",
    "esbuild": "^0.15.8",
    "typescript": "^4.7.3",
    "@vercel/ncc": "^0.34.0"
  }
}